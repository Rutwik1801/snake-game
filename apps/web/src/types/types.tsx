export type CoOrdinateObject = {
  x: number;
  y: number;
};

export type Direction = {
  d: { x: number; y: number };
  u: { x: number; y: number };
  l: { x: number; y: number };
  r: { x: number; y: number };
};
